import { BlogContainerDirectiveDirective } from './blog-container-directive.directive';

describe('BlogContainerDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BlogContainerDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
